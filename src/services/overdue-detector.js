"use strict";const N=s=>String(s||"").trim().toUpperCase().replace(/\s+/g,"_");const paid=new Set(["PAGO","QUITADO","BAIXADO","CANCELADO","CANCELADA"]);const P=v=>{const r=String(v||"");const m=r.match(/^(\d{4})-(\d{2})-(\d{2})/);if(m)return new Date(`${m[1]}-${m[2]}-${m[3]}T00:00:00Z`);return null};const isPaidStatus=s=>paid.has(N(s));const isParcelaOverdue=(p,now=new Date())=>{if(isPaidStatus(p?.status))return false;const st=N(p?.status);if(["ATRASADO","VENCIDO","EM_ATRASO"].includes(st)||/ATRAS|VENCID/.test(st))return true;const d=P(p?.vencimento);if(!d)return false;const t=new Date(now);t.setUTCHours(0,0,0,0);return d<t};module.exports={isParcelaOverdue,isPaidStatus};
